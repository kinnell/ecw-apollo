<div class="task-starred" style="<%= 'visibility: hidden;' if task.completed %>">
	<%= link_to toggle_starred_task_path(task), method: :post do %>
		<i class="fa fa-star<%= '-o' unless task.starred %>"></i>
	<% end %>
</div>
